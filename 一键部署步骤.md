先跑这个指令



```
\& { $content = Get-Content ".\\install.ps1" -Raw -Encoding UTF8; iex $content }
```



然后去`C:\Users\<用户名>\AppData\Local\pnpm` 中运行 `openclaw.ps1`

最后运行
```azure
openclaw onboard --non-interactive --accept-risk
```